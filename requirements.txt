pyserial          # Reads data from Arduino via USB
google-generativeai  # Google Gemini AI API
python-dotenv     # Reads your .env secret key file
vosk              # Speech-to-text (listens to you)
schedule          # Runs tasks on a timer
pandas            # Handles sensor data in tables
requests   